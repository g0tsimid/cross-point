/**
 * A section in a presentation that contains richly formatted text, timing information,
 * and an optional visual aid.
 */
export class PresentationSection {
  text: string
  autoplayLength: number
  sortIndex: number
}